meta {
  name: Service Details (Stream)
  type: http
  seq: 5
}

get {
  url: {{host}}/api/service/stream/:guid/:host/:service
  body: none
  auth: inherit
}

params:path {
  guid: {{lookup_app}}
  service: {{lookup_service}}
  host: {{lookup_host}}
}

settings {
  encodeUrl: true
  timeout: 0
}
