meta {
  name: Service Enable
  type: http
  seq: 6
}

post {
  url: {{host}}/api/service/control/:guid/:host/:service
  body: json
  auth: inherit
}

params:path {
  service: {{lookup_service}}
  host: {{lookup_host}}
  guid: {{lookup_app}}
}

body:json {
  {"action": "enable"}
}

settings {
  encodeUrl: true
  timeout: 0
}
