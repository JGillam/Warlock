meta {
  name: Service Details
  type: http
  seq: 4
}

get {
  url: {{host}}/api/service/:guid/:host/:service
  body: none
  auth: inherit
}

params:path {
  service: {{lookup_service}}
  host: {{lookup_host}}
  guid: {{lookup_app}}
}

settings {
  encodeUrl: true
  timeout: 0
}
