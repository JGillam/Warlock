meta {
  name: DEL Host Firewall rule
  type: http
  seq: 31
}

delete {
  url: {{host}}/api/firewall/:host
  body: json
  auth: inherit
}

params:path {
  host: {{lookup_host}}
}

body:json {
  {
    "action": "ALLOW",
    "to": "1337",
    "proto": "tcp",
    "comment": "Test Port"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
