<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Warlock - Login</title>
	<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
	<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
	<link rel="stylesheet" href="<%= assetUrl('/assets/frontend.css') %>">
	<link rel="stylesheet" href="<%= assetUrl('/assets/themes.css') %>">
	<script src="<%= assetUrl('/assets/common.js') %>"></script>
	<link rel="icon" type="image/webp" sizes="64x64" href="<%= assetUrl('/assets/media/logos/warlock/warlock-lopoly-logo-64x64.webp') %>">
	<link rel="icon" type="image/webp" sizes="128x128" href="<%= assetUrl('/assets/media/logos/warlock/warlock-lopoly-logo-128x128.webp') %>">
	<link rel="icon" type="image/webp" sizes="256x256" href="<%= assetUrl('/assets/media/logos/warlock/warlock-lopoly-logo-256x256.webp') %>">
	<link rel="icon" type="image/webp" sizes="512x512" href="<%= assetUrl('/assets/media/logos/warlock/warlock-lopoly-logo-512x512.webp') %>">
</head>
<body>

<div class="main-container" style="grid-template-columns: 1fr; max-width: 800px;">
	<main class="content-area">
		<div class="content-header">
			<div class="content-header-inner">
				<h2>
					<i class="fas fa-user"></i>
					Login
				</h2>
				<p>Please login to start slinging spells</p>
			</div>
		</div>

		<div class="content-body">
			<% if (typeof(error) !== 'undefined') { %>
				<div class="error-message">
					<i class="fas fa-exclamation-triangle"></i>
					<strong>Error:</strong> <%= error %>
				</div>
			<% } %>

			<form action="/login" method="POST">
				<input type="hidden" name="_csrf" value="<%= csrfToken %>">

				<div class="form-group">
					<label for="admin-username">Your Username</label>
					<input name="username" type="text" id="admin-username" placeholder="Enter username">
				</div>

				<div class="form-group">
					<label for="admin-password">Your Password</label>
					<input name="password" type="password" id="admin-password" placeholder="Enter password">
				</div>

				<% if (twofactor) { %>
					<div class="form-group">
						<label for="authcode">2FA Authentication Code</label>
						<input name="authcode" type="number" id="authcode" placeholder="123456" maxlength="6" pattern="\d{6}">
						<p class="form-help-text">Enter the generated 6-digit code as displayed in your authentication application, (if you have setup 2FA)</p>
					</div>
				<% } %>

				<button type="submit" class="action-save">Login</button>
			</form>
		</div>
	</main>
</div>

<%- include('partials/theme-selector.ejs') %>

</body>
</html>
